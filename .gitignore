# Packages
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip

# JS files will be compiled from ts anyway
src/app/*.js

# Node modules will be installed via npm
node_modules/
